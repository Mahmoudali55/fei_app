import 'package:flutter/material.dart';

class CamerScanScreen extends StatelessWidget {
  const CamerScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
